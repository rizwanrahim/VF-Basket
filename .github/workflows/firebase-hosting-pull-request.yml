# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Update Environment File
        run: |
          echo "Setting environment variables..."
          source src/environments/temp-environment.ts
          echo "GITHUB_PROJECT_ID=${GITHUB_PROJECT_ID}" >> $GITHUB_ENV
          echo "GITHUB_APP_ID=${GITHUB_APP_ID}" >> $GITHUB_ENV
          echo "GITHUB_STORAGE_BUCKET=${GITHUB_STORAGE_BUCKET}" >> $GITHUB_ENV
          echo "GITHUB_API_KEY=${GITHUB_API_KEY}" >> $GITHUB_ENV
          echo "GITHUB_AUTH_DOMAIN=${GITHUB_AUTH_DOMAIN}" >> $GITHUB_ENV
          echo "GITHUB_MESSAGING_SENDER_ID=${GITHUB_MESSAGING_SENDER_ID}" >> $GITHUB_ENV
          echo "GITHUB_MEASUREMENT_ID=${GITHUB_MEASUREMENT_ID}" >> $GITHUB_ENV

      - name: Display Updated Environment File
        run: cat src/environments/environment.ts

      # - name: Install
      #   run: npm install

      # - name: Build
      #   run: npm run build

      # - name: Deploy
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VNF_BASKET }}'
      #     projectId: vnf-basket
